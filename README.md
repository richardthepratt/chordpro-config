# chordpro-config
A nice example ChordPro myconfig.json including a long list of guitar chords, some pre-parsing for Unicode, and some PDF formatting. Also available as a PHP array that generates pretty JSON.
